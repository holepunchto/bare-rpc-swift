{
  "name": "bare-rpc-swift-fixtures",
  "private": true,
  "description": "Bare runtime fixtures for bare-rpc-swift interop tests.",
  "engines": {
    "bare": ">=1.0.0"
  },
  "dependencies": {
    "bare-process": "^4.4.0",
    "bare-rpc": "^1.2.0"
  }
}
